# Order Consumer Service Environment Configuration Example
# Copy this file to .env.order-consumer and modify values as needed

# Environment Settings
ENVIRONMENT=development
LOG_LEVEL=info

# Kafka Topic Configuration  
KAFKA_CONSUMED_TOPICS=order.placed
KAFKA_PRODUCED_TOPICS=service.logs

# Service Configuration
DATABASE_URL=postgres://auction_user:auction_password@localhost:5432/auction_db?sslmode=disable
KAFKA_BROKERS=localhost:9092
CONSUMER_GROUP_ID=order-consumer-group

# Consumer Performance Settings
CONSUMER_BATCH_SIZE=10
CONSUMER_TIMEOUT=10s
WORKER_POOL_SIZE=5

# Database Configuration
DB_MAX_OPEN_CONNS=10
DB_MAX_IDLE_CONNS=2
DB_MAX_LIFETIME=5m
